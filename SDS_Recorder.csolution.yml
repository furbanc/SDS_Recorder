solution:

  created-for: CMSIS-Toolbox@2.7.0
  cdefault:

  target-types:
    # - type: <target_name>
    #   board: <board_name>
    #   variables:
    #     - Board-Layer: <board_layer>.clayer.yml
    #     - Socket-Layer: $SolutionDir()$/Socket/Socket.clayer.yml
        
  build-types:
    - type: Debug
      debug: on
      optimize: none
    - type: Release
      debug: off
      optimize: balanced

  projects:
    - project: ./SDS_Recorder.cproject.yml
